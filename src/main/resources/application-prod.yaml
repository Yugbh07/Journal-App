spring:
  application:
      name: journelApp
  data:
    mongodb:
      uri: mongodb+srv:${MONGODB_URI}
      database: journaldb
      auto-index-creation: true

server:
  port: 8080
  servlet:
    context-path:
